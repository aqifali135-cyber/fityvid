{
  "name": "fityvid",
  "private": true,
  "version": "1.0.0",
  "description": "FityVid — frontend on Vercel, API on a separate host (Render/Railway/VPS).",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  }
}
